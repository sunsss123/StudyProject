[메모리]
힙: 동적할당된 메모리가 할당되는 메모리영역
>> 런타임에 메모리가 할당되는 영역
스택: 정적할당된 메모리가 할당되는 메모리영역
>> 컴파일 이전에 할당된 메모리가 컴파일러에 의해서 관리되는 순서가 정해진 것
컴파일: 코드를 기계어로 번역하는 과정
런타임: 

[자료구조]
힙트리:
스택:

[기초지식]
인코딩 방식
UTF-8:
UTF-8(BOM):
ANSI:
아스키코드:
멀티바이트:
유니코드:
Call by reference(참조에 의한 호출):
Call by value(값에 의한 호출):
Static:
지역변수: 함수나 구문내에 할당된 변수 -> 스택
전역변수: 프로그램이 종료될 때까지 삭제되지 않는 변수 -> ㅡ택
멤버변수: 객체 내에 있는 변수 
콜백함수
함수포인터
델리게이트
포인터
가상함수
메모리주소: RAM에 있는 메모리가 할당되는 위치

[알고리즘]
유한상태머신(FSM):
상태(State):
비헤이비어트리:
A*:

[디자인 패턴]
스테이트패턴:
MVC패턴
>>모델(데이터-> 플레이어 인벤토리 등)
-> 내부스키마
>>뷰(보여주는 부분-> UGUI객체 등)
-> 외부스키마
>>컨트롤러(데이터를 규칙대로 조작하여 뷰에 보여지게 구현-> GUIManager)
-> 개념스키마

[게임수학/물리]
Vector:
-방향:
-거리:
-량:
DeltaTime:
미분:
적분:
강체(리지드바디): 물리에서 힘을 주는 대상
힘: 물체에 가해진 힘의 량
이동: 시간에 따라 위치가 변경되는 것
mass:

[객체지향]
static class:
static member:
-value:
-function:
상속:
다형성
동적바인딩
DAO: 정보를 데이터베이스에서 가져오는 객체
DTO: 데이터베이스 필드의 정보들을 멤버로 가지는 객체


[C++]
클래스: 기본멤버가 private 설계도 
구조체: 기본멤버가 public 
객체(인스턴스): 클래스나 구조체가 메모리에 할당된 것

[C#]
클래스: 가르키는 객체를 변수로 만들고 인스턴스를 할당하거나, 다른 인스턴스를 참조한다.
구조체: 객체를 스택에 할당한다.
인스턴스: 클래스에 사용 가능한 메모리를 할당하는 것. 할당된 메모리를 객체에 넣어야만 참조가 된다.
객체: 클래스를 변수로 선언하여 인스턴스를 할당하거나 참조할 수 있는 자리(참조자, 포인터)

[Unity]
GetComponent:
OnGUI(Method):

[Unreal]
비헤이비어트리:
-테스크:
-
-

[질문]
ANSI로 저장한 한글이 깨지는 이유
>>
vs git 커밋이 permmision denied 에러 발생했다가 재부팅 시 정상 작동하는 이유
>>
독수리가 플레이어의 방향으로 이동하기 위해 필요한 조건은 무엇인가?

